package u.aly;

import java.util.ArrayList;

// compiled from: USession.java
public final class l extends bn implements dc {
    public final void a(bp bpVar, String str) {
        if (bpVar.g == null) {
            bpVar.g = new ArrayList();
        }
        bpVar.g.add(this);
    }
}
