package org.apache.thrift;

import java.io.Serializable;
import org.apache.thrift.protocol.f;

public interface b<T extends b, F> extends Serializable, Comparable<T> {
    void a(f fVar);

    void b(f fVar);
}
